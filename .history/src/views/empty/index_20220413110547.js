import { defineComponent } from 'vue';

import './index.less';

const Layout = defineComponent({
    setup() {
        return () => (
            <div></div>
        );
    }
});

export default Layout;
