import { defineComponent } from 'vue';

import './index.less';

const Home = defineComponent({
    setup() {
        return () => (
            <div class="home">

            </div>
        );
    }
});

export default Home;
