import { defineComponent } from 'vue';

import './index.less';

const Home = defineComponent({
    setup() {
        return () => (
            <div class="home">
                asdsa
            </div>
        );
    }
});

export default Home;
