import { defineComponent } from 'vue';

import './index.less';

const Login = defineComponent({
    setup() {
        return () => (
            <div class="login">
                login
            </div>
        );
    }
});

export default Login;
